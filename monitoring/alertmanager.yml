global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'dssiitbhu@gmail.com'
  smtp_auth_username: 'dssiitbhu@gmail.com'
  smtp_auth_password: 'cxns emds rmjs yugk'
  smtp_require_tls: true

route:
  receiver: email-default
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

templates:
  - '/etc/alertmanager/templates/*.tmpl'

receivers:
  - name: email-default
    email_configs:
      - to: 'saxenarajat499@gmail.com'
        send_resolved: true
        html: '{{ template "custom.email.html" . }}'
